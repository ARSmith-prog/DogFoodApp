<!DOCTYPE html>
<html lang="
" dir=" ltr"=" ltr""="ltr" "">
<head>
  <meta charset="utf-8">
  <title>LogIn</title>
</head>
<nav id="nav" class="navbar navbar-expand-lg fixed-top">
  <div class="container-fluid">

    <div class="navbar-brand order-1 mr-0 ">

      <% if current_user %>

        <%= button_to 'Logout', destroy_user_session_path, method: :delete %>

      <% else %>

        <a type="button" class=" btn btn-outline-dark rounded-pill" href=<%= new_user_registration_path %>>Sign Up</a>
        <a type="button" class=" btn btn-outline-dark rounded-pill" href=<%= new_user_session_path %>>Sign in</a>

      <% end %>

    </div>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item nav-link">
          <a class="navbar-brand" href=<%= pages_home_path %>>
            <%= image_tag("img/logo.png", style: "border-radius: 500px; width: 80px; height: 40px;") %>
          </a>
        </li>
        <li class="nav-item nav-link">
          <a class="nav-link active" aria-current="page" href=<%= pages_home_path %>>Home</a>
        </li>
        <li class="nav-item nav-link">
          <a class="nav-link" href=<%= dogs_path %>>Dogs</a>
        </li>

        <% if current_user&.admin? %>
          <li class="nav-item nav-link">
            <a class="nav-link" href=<%= recipes_path %>>Recipes</a>
          </li>
        <% end %>

      </ul>
    </div>
  </div>
</nav>
<!-- end of nav bar -->

<p id="notice"><%= notice %></p>

<!-- <div id="bannerp" class="text-center my-4"> -->


<% if current_user %>
  <div id="bannerw" style="margin-top:55px; padding-top:30px;" class="container-fluid text-sm-center ">
    <%= link_to 'Add New Dog', new_dog_path, :class => "btn btn-lg btn-warning my-4" %>
  </div>
<% end %>

<div id="bannerp" class="text-center" style="padding-bottom:50px; padding-top:50px;">


  <div class="rounded mx-auto d-block py-4" style=" width:400px;">
    <% if @dog.image.attached? %>
      <%= image_tag(@dog.image, style: 'width:100%') %>
    <% end %>
  </div>

  <p>
    <strong>Name:</strong>
    <%= @dog.title %>
  </p>

  <p>
    <strong>Age:</strong>
    <%= @dog.age %>
  </p>

  <p>
    <strong>Uploaded by user:</strong>
    <%= @dog.user.username %>
  </p>

  <%= link_to_unless(@prev.blank?, "< Previous", @prev, class: "px-2 ") %>

  <%= link_to_unless(@next.blank?, "Next >", @next, class: "px-4") %>
  </br>
  <% if current_user == @dog.user || current_user&.admin %>
    <%= link_to 'Edit', edit_dog_path(@dog), id: "button", class: "btn text-decoration-none my-4 " %>
  <% end %>

  <%= link_to 'View all dogs', dogs_path, id: "button", class: "btn text-decoration-none my-4" %>
</div>

<div id="footer" class="text-center py-4">
  <div>
    <small>&copy; 2021 ChowMeDown | Alannah Smith David</small>
  </div>
</div>
</html>
