<!DOCTYPE html>
<html lang="
" dir=" ltr"=" ltr""="ltr" "">
<head>
  <meta charset="utf-8">
  <title>LogIn</title>
</head>
<nav id="nav" class="navbar navbar-expand-lg fixed-top ">
  <div class="container-fluid">

    <div class="navbar-brand order-1 mr-0 ">

      <% if current_user %>

        <%= button_to 'Logout', destroy_user_session_path, method: :delete %>

      <% else %>

        <a type="button" class=" btn btn-outline-dark btn-light rounded-pill" href=<%= new_user_registration_path %>>Sign
          Up</a>
        <a type="button" class=" btn btn-outline-dark btn-light rounded-pill" href=<%= new_user_session_path %>>Sign
          in</a>

      <% end %>
    </div>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item nav-link">
          <a class="navbar-brand" href=<%= pages_home_path %>>
            <%= image_tag("img/logo.png", style: "border-radius: 500px; width: 80px; height: 40px;") %>
          </a>
        </li>
        <li class="nav-item nav-link">
          <a class="nav-link active" aria-current="page" href=<%= pages_home_path %>>Home</a>
        </li>
        <li class="nav-item nav-link">
          <a class="nav-link" href=<%= dogs_path %>>Dogs</a>
        </li>

        <% if current_user %>
          <li class="nav-item nav-link">
            <a class="nav-link" href=<%= recipes_path %>>Recipes</a>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>

<!-- end of nav bar -->

<div id="bannerw" style="margin-top:55px; padding-top:55px;" class="container-fluid text-center p-5 ">
  <h1 id="heading" class="display-2">Recipes</h1>
  <h4> ChowMeDown exclusive recipes</h4>
  <p>Find the best recipes for your dog</p>

  <p class="pt-4">Search for a specific ingredient:</p>

  <!-- search form -->
  <%= form_for "", url: recipes_path, role: "search", method: :get do %>
    <%= text_field_tag :search, @search_term, placeholder: "e.g.chicken" %>
  <% end %>

  <!-- end search from -->

</div>
<!-- end of header -->

<div id="bannerp" class="text-center" style="padding-bottom: 70px; padding-top: 70px;">
  <table>
    <tbody id="outsidecont">

    <% @recipes.each do |recipe| %>

      <tr id="recipes">

        <td style="width:300px; height:207px;" class="mx-auto d-block">
          <%= image_tag("img/logo.png", style: "width:100%; height: 210px;") %>
        </td>
        <td id="title" class="fst-italic"><%= recipe.title %></td>
        <td class="fst-italic"><%= recipe.description %></td>

        <td id="sidebyside"><%= button_to 'View Recipe', recipe_path(:id => recipe.id), :method => :get, :class => "btn btn-primary btn-sm" %></td>

        <% if current_user&.admin? %>
          <td id="sidebyside"><%= button_to 'Edit', edit_recipe_path(:id => recipe.id), :method => :get, :class => "btn btn-primary btn-sm" %></td>

          <td id="sidebyside"><%= button_to 'Destroy', recipe_path(:id => recipe.id), :method => :delete, :class => "btn btn-primary btn-sm" %></td>

        <% end %>
      </tr>

    <% end %>
    </tbody>
  </table>
</div>

<div id="footer" class="text-center py-3">
  <div>
    <small>&copy; 2021 ChowMeDown | Alannah Smith David</small>
  </div>
</div>
</html>
