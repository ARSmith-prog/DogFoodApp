<nav id="nav" class="navbar navbar-expand-lg fixed-top ">
  <div class="container-fluid">

    <div class="navbar-brand order-1 mr-0 ">

      <% if current_user %>

        <%=button_to 'Logout', destroy_user_session_path, method: :delete %>

      <% else %>

        <a type="button" class=" btn btn-outline-dark btn-light rounded-pill" href=<%=new_user_registration_path%>>Sign Up</a>
        <a type="button" class=" btn btn-outline-dark btn-light rounded-pill" href=<%=new_user_session_path%>>Sign in</a>

      <%end %>
    </div>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item nav-link">
          <a class="navbar-brand" href=<%=pages_home_path%>>
            <%= image_tag("img/logo.png", style: "border-radius: 500px; width: 80px; height: 40px;")%>
          </a>
        </li>
        <li class="nav-item nav-link">
          <a class="nav-link active" aria-current="page" href=<%=pages_home_path%>>Home</a>
        </li>
        <li class="nav-item nav-link">
          <a class="nav-link" href=<%=dogs_path%>>Dogs</a>
        </li>

        <% if current_user %>
          <li class="nav-item nav-link">
            <a class="nav-link" href=<%=recipes_path%>>Recipes</a>
          </li>
        <%end%>
      </ul>
    </div>
  </div>
</nav>

<!-- end of nav bar -->

<div id="bannerp" style="margin-top:55px; padding-top:55px;" class="container-fluid text-sm-center p-5 text-white ">
  <h1>Recipes</h1>

  <h4>Search for a recipe:</h4>

  <%= form_tag(recipes_path, method: :get) do %>
  <%= text_field_tag(:search, params[:search]) %>
  <%= submit_tag ("Search") %>
<% end %>
</div>
<!-- end of header -->

<div id="bannerp" class="text-center">
<table >
  <tbody>

    <% @recipes.each do |recipe| %>
      <tr id="recipes">
        <td id="title"><%= recipe.title %></td>
        <td ><%= recipe.description %></td>
        <td><%= button_to 'Show', recipe_path(:id => recipe.id), :method => :get, :class => "btn btn-warning btn-sm"%></td>
        <% if current_user&.admin? %>
          <td><%= button_to 'Edit', edit_recipe_path(:id => recipe.id), :method => :get, :class => "btn btn-warning btn-sm" %></td>
          <td><%= button_to 'Destroy', recipe_path(:id => recipe.id), :method => :delete, :class => "btn btn-warning btn-sm" %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>
</div>


<div id="footer2" class="text-center pt-3">
<div >
  <small>&copy; 2021 ChowMeDown | Alannah Smith David</small>
</div>
</div>
